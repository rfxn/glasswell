{
  "name": "glasswell-e2e",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Browser-path regression tier. Deliberately its own project: it must not add a dependency to the web bundle's lockfile.",
  "scripts": {
    "test": "node --test",
    "smoke": "node smoke.mjs",
    "status": "node status-surface.mjs"
  },
  "devDependencies": {
    "playwright-core": "^1.62.1"
  }
}
