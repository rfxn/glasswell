# Numeric leaves of `data` that are exempt from carrying a derivation handle
# (SB-07 §10 check 2c). `*` matches one JSON Pointer segment, `**` matches any
# number of them. Pointers are relative to `data`. Every entry states why the
# number is not a figure; a hostile reviewer should be able to read this file and
# see exactly what was exempted.
#
# Not present, deliberately: page-size and query-parameter echoes. This slice does
# not echo request parameters into `data`, so no pattern exempts them — if one is
# ever added it fails the walker until it is exempted here on purpose.
#
# An entry here is not free: `test_no_exemption_covers_a_served_figure` re-walks
# every served figure and fails if any pattern below would have covered one, so a
# broad pattern (`/**`) cannot be used to silence the walker.

# --- identifiers -------------------------------------------------------------
- pointer: /api10
  reason: Identifier. A 10-digit API number is an identity string, not a measurement.
- pointer: /*/api10
  reason: Identifier, in a collection item.
- pointer: /api14
  reason: Identifier. The completion-level API number.
- pointer: /state_code
  reason: FIPS-style state code carried as reported; an identifier, not a quantity.
- pointer: /state
  reason: API state code carried as reported; an identifier, not a quantity.
- pointer: /states/*/code
  reason: API state code carried as reported; an identifier, not a quantity.
- pointer: /jurisdictions/*/code
  reason: API state code carried as reported; an identifier, not a quantity.
- pointer: /basins/*/state_code
  reason: API state code on a per-basin summary row; an identifier, not a quantity.
- pointer: /*/identity/prefix
  reason: >-
    The two leading digits of every API-10 in a jurisdiction: an identifier's prefix, not a measured quantity.
- pointer: /county_code_at_permit
  reason: County code as reported at permit; an identifier, not a quantity.
- pointer: /*/county_code_at_permit
  reason: County code, in a collection item.
- pointer: /ndic_file_no
  reason: The NDIC file number is the regulator's identifier for the well.
- pointer: /cohorts/*/cohort_year
  reason: >-
    Cohort key. The year that identifies a cohort of wells; an identifier for the group, not
    a quantity measured about it. Which year it is - spud or completion anchor - is stated
    by cohort_basis and ruled by cohort_key_rule.
- pointer: /population_scope/states_served/*
  reason: >-
    API state prefix naming what this population covers; an identifier, not a quantity. Same
    class as /state_code.
- pointer: /*/ndic_file_no
  reason: The NDIC file number, in a collection item.

# --- coordinates -------------------------------------------------------------
- pointer: /surface_point/lon
  reason: Geometry, not a figure. Storage CRS is EPSG:4326 and is stated alongside.
- pointer: /surface_point/lat
  reason: Geometry, not a figure. Storage CRS is EPSG:4326 and is stated alongside.

# --- counts and shares -------------------------------------------------------
- pointer: /distinct_values
  reason: >-
    Cardinality of a value list, not a measured or modelled petroleum figure. How many
    operators exist is a fact about the vocabulary; how many wells they hold is the figure
    beside it, and that one carries a handle.
- pointer: /remainder/values
  reason: >-
    Cardinality of the value list below the served cut. The wells those values hold is the
    figure beside it and carries a handle.
- pointer: /top
  reason: >-
    Echo of the requested page cut; it counts nothing in the data. Served because a reader
    must be able to see the list is a cut of fifteen without counting the array.
- pointer: /lateral_count
  reason: A count of geometry rows, not a measured quantity.
- pointer: /total
  reason: Row count of the quarantine population being summarised.
- pointer: /groups/*/count
  reason: Row count per group.
- pointer: /groups/*/share
  reason: Count divided by count; it carries no unit and no vintage.
- pointer: /support_distribution/classes/*
  reason: >-
    How many cohorts fall in each support band. A count of rows in this response, taken over
    figures that each carry their own handle, not a measured quantity of its own.
- pointer: /cap/limit
  reason: >-
    The cut a status history was taken at. A property of the response, not of the well: New
    Mexico holds 15,590 distinct filed effective dates across its population and its fullest
    single well carries 15 of them, 248 wells carry more than ten, and ten is a readable
    answer with the remainder counted beside it.
- pointer: /cap/returned
  reason: How many status-history rows this response carries.
- pointer: /cap/total
  reason: >-
    How many effective-dated headers the well carries at this vintage. Served so a short list
    is never read as a short life; a count of rows in this response's own population, not a
    measured petroleum quantity.
- pointer: /cap/withheld
  reason: Status-history rows the cap held back. Never rows nobody has.
- pointer: /basin_context/basin_overlap
  reason: >-
    How many published basin polygons contain this well's answering geometry. A count of
    boundary rows, served so a reader can see where the publisher's own polygons overlap;
    not a measured petroleum quantity.
- pointer: /occurrence_count
  reason: How many fetches re-presented this rejected row (SB-07 §8.1).
- pointer: /*/occurrence_count
  reason: Occurrence counter, in a collection item.
- pointer: /sources/*/manifest_count
  reason: Count of registered manifests per source on the health page.
- pointer: /datasets/*/metrics/*/value
  reason: >-
    Operational inventory from a timed status snapshot; its grain, precision and observation
    time are stated alongside.
- pointer: /platform/schema_version
  reason: Database migration identity, not a measured petroleum quantity.
- pointer: /platform/database_bytes
  reason: Physical PostgreSQL storage inventory, not a petroleum figure.
- pointer: /*/alias_count
  reason: Reference-row cardinality, not a measured or modelled petroleum figure.
- pointer: /neighbors/*/neighbor_api10
  reason: API-10 identifier on a physical-neighbour row, not a measurement.
- pointer: /*/output_rows
  reason: >-
    Rows a derivation recorded when it wrote its output, in the derivation
    collection. Same class as /output/rows on the expanded record.
- pointer: /entries/*/n_words
  reason: Token count the highlighter uses to build its trie (SB-04 §6.2).
- pointer: /expires_in
  reason: Lifetime in seconds of a CSRF token, a protocol parameter of the exchange.
- pointer: /*/sessions_live
  reason: Live sessions an account holds, a state counter rather than a measured quantity.

# --- spine metadata ----------------------------------------------------------
- pointer: /error_codes/*/status
  reason: An HTTP status code in the error index.
- pointer: /status
  reason: An HTTP status code on the error-type description.
- pointer: /chains/*/depth
  reason: Graph depth of the resolved chain, not data.
- pointer: /chains/*/nodes/*/output/rows
  reason: Row count a derivation recorded when it wrote its output.
- pointer: /chains/*/nodes/*/bytes
  reason: Byte length of the fetched artifact, recorded on the manifest.
- pointer: /output/rows
  reason: Row count a derivation recorded when it wrote its output.
- pointer: /duration_ms
  reason: Wall-clock cost of the recorded derivation.
- pointer: /inputs/*/ord
  reason: Ordinal position of a derivation input.
- pointer: /rules/*/applied_rows
  reason: How many rows a conformance rule touched during that derivation.
- pointer: /applied_by/*/applied_rows
  reason: Reverse index of rows touched, per citing derivation.
- pointer: /bytes
  reason: Byte length of the fetched artifact, recorded on the manifest.
- pointer: /*/bytes
  reason: Byte length, in a manifest collection item.

- pointer: /producing_window/months
  reason: >-
    Length of the producing window in months, read from cr_producing_window_1. A
    parameter of the classing, not an observation it stands behind.

# --- verbatim registry and rejected input ------------------------------------
- pointer: /params/**
  reason: >-
    The recorded call a derivation was made with, served verbatim from
    lineage.derivations. Its numbers are run inputs — EPSG codes, bounds, thresholds
    — and they are part of the derivation's content address, not an observation it
    stands behind. Same class as /spec/**.
- pointer: /output/partition/**
  reason: >-
    The partition key a derivation wrote its output under, served verbatim from
    lineage.derivations. It is part of the derivation's content address — a state
    API prefix, a source id, a vintage — not an observation it stands behind. Same
    class as /params/**.
- pointer: /spec/**
  reason: >-
    Conformance-rule spec, served verbatim from the registry. Its numbers are rule
    parameters (scales, factors, bounds), not served figures.
- pointer: /*/spec/**
  reason: Conformance-rule spec, in a collection item.
- pointer: /row_payload/**
  reason: >-
    The rejected source row, served verbatim as evidence (SB-07 §8). It is the input
    that failed a rule, not a number the system stands behind.

# --- modeling control --------------------------------------------------------
- pointer: /splits/*/horizon_months
  reason: >-
    Horizon of a persisted temporal split, read from the split object. A parameter of
    the split set, not an observation it stands behind.
- pointer: /coverage/control_contract/**
  reason: >-
    The pinned control contract served verbatim from the artifact's coverage document.
    Its numbers are build parameters — minimum peers, vintage window, quantile levels —
    not figures the control stands behind. Same class as /spec/**.
- pointer: /artifact_sha256/*
  reason: >-
    Content address of a published artifact. A digest is an identity string, not a
    measurement, and a hex digest with no letter in it reads to the walker as a number.
- pointer: /rows/*
  reason: >-
    Rows each pinned artifact recorded when it was written. Same class as /output/rows
    on the derivation record.
- pointer: /coverage/acceptance/*/minimum
  reason: >-
    Floor the acceptance gate required, recorded with the artifact. A threshold the
    build was judged against, not an observation it stands behind.
- pointer: /coverage/acceptance/*/maximum
  reason: >-
    Ceiling the acceptance gate allowed, recorded with the artifact. A threshold the
    build was judged against, not an observation it stands behind.
- pointer: /horizon_months
  reason: >-
    Horizon of the persisted temporal split this control was built on. A parameter of
    the split, not an observation the control stands behind.
- pointer: /series/month_index/*
  reason: >-
    Producing-month index, the axis the curve is plotted against. It is the row key,
    not a value.
- pointer: /available_origins/*/horizon_months
  reason: >-
    Horizon of another persisted split for this subject, offered for navigation.
- pointer: /series/control_unavailable_reasons/*/*
  reason: >-
    Why the peer ladder terminated for a subject, served verbatim from the control. A
    vocabulary term, not a number.
- pointer: /series/api10/*
  reason: Identifier, on the row axis of an aligned-array page.
- pointer: /area
  reason: >-
    Area bucket the subject falls in, a label the peer ladder groups by. A zero-padded
    code, not a measurement.
- pointer: /series/area/*
  reason: Area bucket, in an aligned-array page.

# --- the scheduled-job registry ----------------------------------------------
# Operational bounds and run evidence. None of these is a petroleum quantity, and
# none resolves to a derivation: /v1/schedules serves a decision reference, not a
# figure, so a handle here would advertise a resolution that does not exist. The
# root form precedes its /*/ twin, as everywhere above, so the substantive reason
# is the one the document publishes.
- pointer: /cadence/interval_seconds
  reason: >-
    How often a job is asked to run, in seconds. A cadence is a scheduling decision recorded as
    a conformance rule, not a measured quantity.
- pointer: /*/cadence/interval_seconds
  reason: Cadence interval, in a collection item.
- pointer: /cadence/monthly_on_day
  reason: >-
    The calendar day a monthly cadence fires on. A position in the month, not a measurement.
- pointer: /*/cadence/monthly_on_day
  reason: Calendar day of a monthly cadence, in a collection item.
- pointer: /limits/timeout_seconds
  reason: >-
    The time ceiling a job's transient unit is given, bounded by the scheduler unit's own
    budget. An operational bound, not a measured quantity.
- pointer: /*/limits/timeout_seconds
  reason: Job time ceiling, in a collection item.
- pointer: /last_runs/*/duration_seconds
  reason: >-
    Wall time between a run's start and its close, from operational run evidence; its grain and
    observation time are stated beside it.
- pointer: /last_runs/*/exit_status
  reason: A process exit status. An outcome code, not a quantity.
- pointer: /last_runs/*/memory_peak_bytes
  reason: >-
    Peak resident bytes a run reached, read from systemd, and absent rather than zeroed where
    systemd is older than 254. Operational inventory, not a petroleum figure.
- pointer: /jobs/*/duration_seconds
  reason: >-
    How long a scheduled job ran, on the status page's job rows. Operational inventory from a
    timed snapshot, not a petroleum figure.
# One entry and not three: the class domain is served in `meta` and the walker reads `data`,
# so an exemption for its two integers could never match.
- pointer: /*/map/wells_draw_order
  reason: >-
    Where a jurisdiction's Wells row sits in the layer panel. A real per-row integer that
    ranks layers, not a measurement of anything: disposal wells sit between two of them.

# --- the allocation, and what is method rather than measurement ---------------
# An allocated share is a figure and carries a handle like any other. What is exempt
# here is the arithmetic that produced it — a divisor is part of the method — and the
# registry key the residual ledgers are reported for.
- pointer: /series/oil_bbl_eligible_wells_by_month/*
  reason: >-
    Divisor. The count of wells a share was computed over is part of the method, not a
    measured quantity of anything.
- pointer: /series/gas_mcf_eligible_wells_by_month/*
  reason: >-
    Divisor. The count of wells a share was computed over is part of the method, not a
    measured quantity of anything.
- pointer: /series/oil_bbl_shares_by_month/*
  reason: >-
    Cardinality. The count of lease records a point was summed over is part of the method,
    not a measured quantity of anything; each share carries its own handle.
- pointer: /series/gas_mcf_shares_by_month/*
  reason: >-
    Cardinality. The count of lease records a point was summed over is part of the method,
    not a measured quantity of anything; each share carries its own handle.
- pointer: /blocks/*/degraded_at
  reason: >-
    The threshold the Status check compares the unallocated share against, read from
    cr_tx_allocation_v0_1's own spec. A parameter of the decision, not an observation the
    ledger stands behind; the share beside it is the figure.
